# A file viewer + text insertion
Run with `(vico-term:main FILENAME)`

## todo

* deletion/ui fixes soon
* fix unicode widths, split into library
* incremental search
* multiline highlighting & bracket matching
